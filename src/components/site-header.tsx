"use client";
import Link from "next/link";
import { Heart, Menu, MessageCircle, X } from "lucide-react";
import { useState } from "react";
import { whatsappUrl } from "@/lib/whatsapp";

export function SiteHeader() { const [open,setOpen]=useState(false); const links=[['Catálogo','/catalogo'],['Kits','/kits'],['Envíos','/envios'],['Preguntas','/#faq']]; return <header className="sticky top-0 z-50 border-b border-white/10 bg-velvet/90 backdrop-blur"><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-5"><Link href="/" className="font-display text-2xl tracking-wide">Velvet <span className="text-rose">Room</span></Link><nav className="hidden gap-8 text-sm text-white/70 md:flex">{links.map(([n,h])=><Link key={n} href={h} className="transition hover:text-gold">{n}</Link>)}</nav><div className="flex items-center gap-4"><Link href="/favoritos" aria-label="Favoritos"><Heart size={18}/></Link><a href={whatsappUrl()} target="_blank" aria-label="WhatsApp" className="hidden items-center gap-2 rounded-full bg-rose px-4 py-2 text-xs font-semibold md:flex"><MessageCircle size={15}/> Contactar</a><button aria-label="Abrir menú" className="md:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></div>{open&&<nav className="flex flex-col gap-5 border-t border-white/10 px-5 py-6 text-sm md:hidden">{links.map(([n,h])=><Link onClick={()=>setOpen(false)} key={n} href={h}>{n}</Link>)}</nav>}</header> }
