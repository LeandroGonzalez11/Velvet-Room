import { SiteHeader } from "@/components/site-header"; import { SiteFooter } from "@/components/site-footer"; import { Box, MapPin, ShieldCheck } from "lucide-react";
export default function ShippingPage(){return <><SiteHeader/><main className="mx-auto min-h-screen max-w-5xl px-5 py-20 md:py-32"><p className="eyebrow">Velvet Room</p><h1 className="mt-4 font-display text-6xl md:text-8xl">Tu privacidad,<br/><em className="text-rose">nuestro cuidado.</em></h1><p className="mt-8 max-w-xl text-lg leading-8 text-white/60">Cada pedido se prepara con discreción y atención personal, para que solo tengas que disfrutar de tu momento.</p><div className="mt-20 grid gap-px bg-white/10 md:grid-cols-3">{[[Box,"Empaque discreto","Sin referencias al contenido ni a Velvet Room en el exterior."],[MapPin,"Todo Uruguay","Trabajamos con agencias y coordinamos la opción más conveniente."],[ShieldCheck,"Retiro por locker","Cuando esté disponible, coordinamos retiros prácticos y privados."]].map(([Icon,title,text])=>{const I=Icon as typeof Box;return <div className="bg-velvet p-8" key={title as string}><I className="text-gold"/><h2 className="mt-5 font-display text-2xl">{title as string}</h2><p className="mt-3 text-sm leading-6 text-white/55">{text as string}</p></div>})}</div></main><SiteFooter/></>}
